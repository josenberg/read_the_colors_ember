import { makeSquares } from '../../../helpers/make-squares';
import { module, test } from 'qunit';

module('Unit | Helper | make squares');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = makeSquares(42);
  assert.ok(result);
});
